.env
gen-lang-client-*.json
*-service-account*.json
client_secret_*.json
*token*.json
web/node_modules/
web/.next/
web/.env.local
web/.env.*.local
.venv/
venv/
.tools/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# User documents, vector database, and local metadata.
data/documents/*
data/chroma/*
data/document_index.json
!data/documents/.gitkeep
!data/chroma/.gitkeep

# NHRC Obsidian index/content: checked 2026-08-03, no complainant/respondent
# names found in the parsed text (see HANDOFF.md) - committed intentionally
# so the deployed site has real search data.
# data/nhrc_documents/ (raw scanned PDFs, ~1.4GB) stays out: unverified for
# personal data, and too large for git/Vercel regardless - needs a real file
# storage solution (S3/Supabase storage/Vercel Blob), not git.
data/nhrc_index.db
data/nhrc_index.db-journal
data/nhrc_documents/

# OS/editor noise.
.DS_Store
Thumbs.db
.vscode/
.idea/
